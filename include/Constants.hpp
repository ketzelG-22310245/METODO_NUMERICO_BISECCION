#pragma once

class Constants
{
private:
    /* data */
public:
    Constants(/* args */) {}
    constexpr static int SCREEN_WIDTH = 800;
    constexpr static int SCREEN_HEIGHT = 600;
    ~Constants() {}
};